#!/usr/bin/env bash

case "$BLOCK_BUTTON" in
  1|2|3) 
    i3-msg 'exec gsimplecal' > /dev/null ;;
esac

date=$(date '+%d %b, %Y - %H:%M')

echo "<span lang='&#xf240;' foreground='#BBCCDD' face='Font Awesome'>  $date</span>"
