# Don't print a new line at the start of the prompt
add_newline = false

# Make prompt a single line instead of two lines
[line_break]
disabled = true

# Replace the "❯" symbol in the prompt with "➜" ✗  ❮
# [character]
# success_symbol = "[\\$](green)"
# error_symbol = "[✗ \\$](#bf616a)"
# vicmd_symbol = "[ \\$](#5e81ac)"

[directory]
# format = "[$path$read_only[\\]](#bf616a)](182)($read_only_style)"
truncation_length = 1
# truncation_symbol = "…/"
read_only = " 🔒" 

[git_branch]
symbol = " "
